"""MCP server entry point.

Placeholder scaffold (M0). The ``search_hackernews`` tool and stdio server
wiring land in M1; ``get_hackernews_thread`` lands in M3.
"""

from __future__ import annotations


def main() -> None:
    """Console-script entry point for the ``hackernews-mcp`` server.

    The real stdio MCP server is wired up in M1. For now this is a placeholder
    so the package installs and the entry point resolves.
    """
    raise SystemExit(
        "hackernews-mcp: server not implemented yet (lands in M1). "
        "See https://github.com/ccozad/hackernews-mcp/issues/2"
    )


if __name__ == "__main__":
    main()
